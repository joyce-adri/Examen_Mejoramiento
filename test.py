# -*- coding: utf-8 -*-
from __future__ import unicode_literals

import unittest
import funcionalidad


class Test(unittest.TestCase):
    # >>>>>> Fernando Sánchez <<<<<<#
    # >>>>>> Estructura para la codificacion de los casos de prueba <<<<<<
    # Reemplazar EL ID por el numero correspondiente del caso de prueba

    def test_prueba_1(self):
        # dep, ec, ce, at, ava, edad
        result_val = funcionalidad.aprobarPrestamo()
        expected_val = ""
        self.assertEquals(result_val, expected_val)

    def test_prueba_2(self):
        # dep, ec, ce, at, ava, edad
        result_val = funcionalidad.aprobarPrestamo()
        expected_val = ""
        self.assertEquals(result_val, expected_val)

    def test_prueba_3(self):
        # dep, ec, ce, at, ava, edad
        result_val = funcionalidad.aprobarPrestamo()
        expected_val = ""
        self.assertEquals(result_val, expected_val)

    def test_prueba_4(self):
        # dep, ec, ce, at, ava, edad
        result_val = funcionalidad.aprobarPrestamo()
        expected_val = ""
        self.assertEquals(result_val, expected_val)

    def test_prueba_5(self):
        # dep, ec, ce, at, ava, edad
        result_val = funcionalidad.aprobarPrestamo()
        expected_val = ""
        self.assertEquals(result_val, expected_val)

    def test_prueba_6(self):
        # dep, ec, ce, at, ava, edad
        result_val = funcionalidad.aprobarPrestamo()
        expected_val = ""
        self.assertEquals(result_val, expected_val)

    def test_prueba_7(self):
        # dep, ec, ce, at, ava, edad
        result_val = funcionalidad.aprobarPrestamo()
        expected_val = ""
        self.assertEquals(result_val, expected_val)


if __name__ == '__main__':
    unittest.main()